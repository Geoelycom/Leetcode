# Backend Configuration
FLASK_APP=app.py
FLASK_ENV=development
DATABASE_URL=sqlite:///./database/app.db

# This is a hardcoded secret for demonstration (Configuration Management Bug)
API_KEY_WEATHER=SUPER_SECRET_WEATHER_API_KEY
EXTERNAL_WEATHER_API_URL=https://api.openweathermap.org/data/2.5/weather
# For the API Misuse bug, we'll use a mock or a real (but free tier) API.
# If using a real API, ensure the key is valid and the URL is correct.
# For this example, we'll assume openweathermap and the 'q' parameter. 